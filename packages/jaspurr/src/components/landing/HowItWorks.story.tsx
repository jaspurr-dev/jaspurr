import type {Story} from '@/sandbox/types';
import {HowItWorks} from './HowItWorks';

export const story: Story = {
    name: 'HowItWorks',
    layer: 'components',
    render: () => <HowItWorks />,
};
