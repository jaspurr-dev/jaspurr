import type {Story} from '@/sandbox/types';
import {RolesSection} from './RolesSection';

export const story: Story = {
    name: 'RolesSection',
    layer: 'components',
    render: () => <RolesSection />,
};
