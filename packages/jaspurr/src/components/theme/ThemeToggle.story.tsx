import type {Story} from '@/sandbox/types';
import {ThemeToggle} from './ThemeToggle';

export const story: Story = {
    name: 'ThemeToggle',
    layer: 'components',
    render: () => <ThemeToggle />,
};
